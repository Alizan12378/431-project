# 431-project

sudo apt update
<p><img width="300" height="24" alt="圖片" src="https://github.com/user-attachments/assets/9fefa65b-92be-43e5-a2c0-57b24a90b8ca" />
</p>

sudo apt install -y curl gnome-terminal git
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
sudo npm install -g ganache

#open terminal in the ~/Home/Downloads
#unzip the zip file
#change the start_web3.sh BASE_DIR to new directory

cd ~/Downloads/'newdonation-truffle (2)'/newdonation-truffle/donation-truffle
sudo chmod 777 *
cd ~/Downloads/'newdonation-truffle (2)'/newdonation-truffle/donation-truffle/eth-balance-checker
sudo rm -rf node_modules package-lock.json
npm cache clean --force
npm install
chmod +x node_modules/.bin/react-scripts


ch ~/Downloads/'newdonation-truffle (2)'/newdonation-truffle/donation-truffle
./start_web3.sh
